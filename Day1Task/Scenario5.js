const array=[5,3,2,8,9];

//can use the let to declared the index variable

for (let i=0; i < array.length; i++){
    console.log(array[i]);
}
//The Variable i only declared in loop because let is only accessible within the loop
//console.log(i); //reference error is comming because i is not accessble
